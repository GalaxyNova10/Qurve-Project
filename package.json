{
  "name": "qurve-project-root",
  "version": "1.2.0",
  "private": true,
  "scripts": {
    "setup:backend": "cd qubo-backend && py -3.11 -m pip install --upgrade pip && py -3.11 -m pip install fastapi uvicorn -r requirements.txt -e .",
    "setup:frontend": "cd app && npm install",
    "setup": "npm run setup:backend && npm run setup:frontend",
    "backend": "cd qubo-backend && py -3.11 -m uvicorn main:app --reload --port 8000",
    "frontend": "cd app && npm run dev",
    "dev": "concurrently --kill-others -n \"backend,frontend\" -c \"blue,green\" \"npm run backend\" \"npm run frontend\"",
    "start": "npm run dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
